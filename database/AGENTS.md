# Database & Schema — Agent Instructions

## Domain Constraints
- **Migrations**: Always provide a `down()` method if possible. Use descriptive column names.
- **Seeders**: Use Factories for generating test/dummy data.
- **Naming**: Database tables should be plural `snake_case`.
- **Integrity**: Use foreign key constraints and indexes where appropriate.
