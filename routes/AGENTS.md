# Routing — Agent Instructions

## Domain Constraints
- **Web Routes**: Organize in `routes/web.php` or `routes/settings.php`.
- **API Routes**: Ensure proper middleware (auth, throttle).
- **Console**: Define commands in `routes/console.php`.
- **Naming**: Use named routes for all links.
